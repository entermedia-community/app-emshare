{
    "name": "search${module.id}",
    "description": "Can search the $module.name table based on keywords",
    "parameters": {
        "type": "object",
        "properties": {
            "keywords": {
                "type": "string"
            }
        },
        "required": [
            "keywords"
        ]
    }
}
