{
    "name": "search${module.id}",
	#set($description = "Can search the ${module.name}")
	#ifnotnull($module.aisearch_contains)
		#set($description = "${description} ${module.aisearch_contains}")
	#end
    "description": "${description}",
    "parameters": {
        "type": "object",
        "properties": {
            "keywords": {
                "type": "string"
            }
			##loop over Summary View
			
			
        },
        "required": [
            "keywords"
        ]
    }
}
