{
  "status": "ok",
  "data": {
    "scenarios": [
      #foreach($scenario in ${scenarios})
        #if($foreach.count > 1),#end
        #json($scenario $scenariosearcher.getPropertyDetails())
      #end
    ]
  }
}