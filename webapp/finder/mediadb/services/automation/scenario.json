{
  "status": "ok",
  "data": {
    "scenario": {
      "id": #jesc("${scenario.id}"),
      "name": #jesc("${scenario.name}")
    },
    "agents": [
      #foreach($agent in ${agents})
        #if($foreach.count > 1),#end
		{
        #jsonpartial($agent $agentenabledsearcher.getPropertyDetails())
		,
			#set( $position = $automationmanager.getPosition( $agent.getId() ) )
			"postion": 	#json($position $automationpositionsearcher.getPropertyDetails())
		}
      #end
    ]
  }
}