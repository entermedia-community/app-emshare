{
  "status": "ok",
  "data": {
    "scenario": {
      "id": #jesc("${scenario.id}"),
      "name": #jesc("${scenario.name}")
    },
    "agents": [
      #foreach($agent in ${agents})
        #if($foreach.count > 1),#end
        #json($agent $agentenabledsearcher.getPropertyDetails())
      #end
    ]
  }
}