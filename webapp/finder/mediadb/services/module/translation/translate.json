{
  "response":
  {
    "status":"ok",
    "id": "$sourcelang"
  },
  "data": {
    #foreach( $translation in $translations.keySet() ) 
    #if( ${foreach.count} != 1 ), #end
    "$translation": #jesc($translations.get($translation))
    #end
  }
}
