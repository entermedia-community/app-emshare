{
	"type": "object",
	"description": "You are an assistant that maps natural language requests to a specific creation task. Only required if next_step is 'parseCreationParts'.",
	"properties": {
		"creation_task": {
			"type": "string",
			"description": "A semantic label for the creation task the user wants to perform.",
			"enum": ["create an image", "create a record"]
		},
		"creation_prompt": {
			"type": "string",
			"description": "The detailed prompt or description provided by the user for the creation task."
		}
	},
	"required": ["creation_task", "creation_prompt"]
}
