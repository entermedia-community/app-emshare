{
	"type": "object",
	"description": "You are an assistant that maps natural language requests to a specific workflow and describes the intent. Only required if next_step is 'parseWorkflowParts'.",
	"properties": {
		"workflow": {
			"type": "string",
			"description": "The workflow to be executed.",
      "enum": [
        #foreach($workflow in $schema.workflows)
          #if($foreach.count > 1),#end 
          #jesc("$workflow.getId()|$workflow.getName()")
        #end
      ]
		},
		"workflow_intent": {
      "type": "string",
      "description": "A brief description of the user's intent for this workflow."
    }
	},
	"required": ["workflow", "workflow_intent"]
}
