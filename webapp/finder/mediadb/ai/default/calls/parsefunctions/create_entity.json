{
	"type": "object",
	"description": "Details about the entity creation request. Only required if request_type is 'create_entity'. Always ask for confirmation with 'Are you sure you want to create 'entity_name' in module 'module_id'?' before proceeding.",
	"properties": {
		"entity_name": {
			"type": "string",
			"description": "Name of the entity to be created. Should be capitalized."
		},
		"module_id": {
			"type": "string",
			"description": "ID of the module where the entity should be saved in.",
			"enum": [
        #foreach($module in $schema.modules)
          #if($foreach.count > 1),#end 
					#if($module.getId() != "asset")
						#jesc("$module.getId()|$module.getName()")
					#end
        #end
      ]
		}
	},
	"required": ["entity_name", "module_id"]
}
