{
	"type": "function",
	"function": {
		"name": "create_${createtype}",
		"description": "Use this tool when the user is trying to create a ${createtype}.",
		"parameters": {
			"type": "object",
			"properties": {
				"name": {
					"type": "string",
					"description": "The capitalized name of the ${createtype}."
				}
			},
			"required": ["name"]
		}
	}
},
{
	"type": "function",
	"function": {
		"name": "play_${createtype}",
		"description": "Use this tool when the user is beginning a ${createtype}.",
		"parameters": {
			"type": "object",
			"properties": {
				"name": {
					"type": "string",
					"description": "The name of the ${createtype} being started."
				}
			},
			"required": ["name"]
		}
	}
}