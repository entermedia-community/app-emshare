{
	"model": "$!{model}",
	"messages": [
		{
			"role": "system",
			"content": "You are a decision-making assistant for an intelligent agent workflow. Your task is to analyze the result of the previous agent's execution and determine which agent should be called next based on the provided options. You will receive a list of potential agents to call, along with the output from the previous agent. Your response should indicate which agent to call next and provide any necessary parameters for that agent."
		},
		{
			"role": "user",
			"content": #jesc("
Previous Agent: $!{agentid}
Previous Agent's Output: $!{agentoutput}
")
		}
	],
	"tools": [
	#foreach($agentenabled in $enabledchildren)
		#if($foreach.count>1)
		,
		#end
		{
			"type": "function",
			"function": {
				"name": "$!{agentenabled.id}",
				"description": "$!{agentenabled.llmprompt}"
				#set($params = $agentenabled.getAgentParameterStructure())
				#if($params.size() > 0)
					,
					"parameters": {
						"type": "object",
						"properties": {
							#foreach($param in $params)
								#if($foreach.count>1)
								,
								#end
								"${param.id}": {
									"type": "${param.data_type}",
									"description": "${param.description}",
									#ifnotnull($param.enum)
									,
									"enum": ${param.enum}
									#end
								}
							#end
						}
					}
				#end
			}
		}
	#end
	],
	"tool_choice": "required"
}
