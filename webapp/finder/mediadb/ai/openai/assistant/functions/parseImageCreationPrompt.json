{
	"name": "parseImageCreationPrompt",
	"description": "Used to extract a text prompt and art style from a user's request to create an image.",
	"parameters": {
		"type": "object",
		"properties": {
			"prompt": {
				"type": "string",
				"description": "The text prompt to create an image from"
			},
			"style": {
				"type": "string",
				"description": "The art style to use for the image"
			}
		},
		"required": ["prompt", "style"]
	}
}
