{
	"name": "createImage",
	"description": "Used to extract a text prompt from a user's request to create an image. Use the exact prompt provided by the user. Do not add or ask for any additional information or context.",
	"parameters": {
		"type": "object",
		"properties": {
			"prompt": {
				"type": "string",
				"description": "The text prompt to create the image from"
			},
			"filename": {
				"type": "string",
				"description": "The name of the image file to be created, without an extension"
			}
		},
		"required": ["prompt", "filename"]
	}
}
