{
	"name": "createImage",
	"description": "Make a concise image creation prompt from user's request. Give it a short filename without extension.",
	"parameters": {
		"type": "object",
		"properties": {
			"prompt": {
				"type": "string",
				"description": "The prompt to create the image"
			},
			"filename": {
				"type": "string",
				"description": "The name of the image file to be created, without an extension"
			}
		},
		"strict": true,
		"additionalProperties": false,
		"required": ["prompt", "filename"]
	}
}
