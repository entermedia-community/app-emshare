{
	"model": "${model}",
	"messages": [
		{
			"role": "system",
			"content": [
				{
					"type": "text",
					"text": #jesc("${fieldparams.aicreationdescription}.

Format your response in JSON according to the response structure below:
$structure
")
				}
			]
		},
		{
			"role": "user",
			"content": [
				{
					"type": "text",
					"text":  "$fieldparams.aicreationcommand
#foreach($field in $contextfields)
#if($foreach.count>1),\n#end
${field.label}: ${field.text}
#end"
				}
			]
		}
	]
}
